import React from 'react'

const Form = () => {
  return (
    <div>
        <form action={}>
            <input type="text" name='name' />
            <input type="email" name='email' />
        </form>
    </div>
  )
}

export default Form;